// Re-export the actual React component implementation from the .tsx file
// so imports that resolve to this module still receive the default export.
export { default } from "./particle-background.tsx"
